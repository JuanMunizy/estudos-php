<?php

// >>> SEU CÓDIGO AQUI <<<
// Escreva seu codigo aqui. Exemplos na sessao abaixo (descomente pra testar).





/* ==========================================================================
   REFERENCIA - ARRAYS
   ==========================================================================

// Array indexado (lista comecando do 0)
$frutas = ["maca", "banana", "laranja"];
echo $frutas[0];          // maca
$frutas[] = "uva";        // adiciona no final

// Array associativo (chave => valor)
$pessoa = [
    "nome" => "Joao",
    "idade" => 30
];
echo $pessoa["nome"];     // Joao

// Array multidimensional (array dentro de array)
$alunos = [
    ["nome" => "Ana", "nota" => 9.5],
    ["nome" => "Carlos", "nota" => 7.8]
];
echo $alunos[0]["nome"];  // Ana

// Funcoes uteis:
// count($arr)          - tamanho do array
// in_array($valor, $arr) - valor existe?
// array_key_exists("chave", $arr) - chave existe?
// array_merge($a, $b)  - une dois arrays
// array_push($arr, $item) - adiciona no final
// array_pop($arr)      - remove do final
// array_shift($arr)    - remove do inicio
// array_unshift($arr, $item) - adiciona no inicio
*/ 
?>