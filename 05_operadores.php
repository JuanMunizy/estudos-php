<?php

// >>> SEU CÓDIGO AQUI <<<
// Escreva seu codigo aqui. Exemplos na sessao abaixo (descomente pra testar).





/* ==========================================================================
   REFERENCIA - OPERADORES
   ==========================================================================

// --- Aritmeticos ---
echo 10 + 3;   // soma: 13
echo 10 - 3;   // subtracao: 7
echo 10 * 3;   // multiplicacao: 30
echo 10 / 3;   // divisao: 3.333...
echo 10 % 3;   // resto: 1
echo 10 ** 3;  // potencia: 1000

// --- Atribuicao com operacao ---
$x = 5;
$x += 3;  // $x = $x + 3 -> 8
$x -= 2;  // $x = $x - 2 -> 6

// --- Comparacao (importante: == vs ===) ---
var_dump(10 == "10");   // true  (iguais, tipos diferentes)
var_dump(10 === "10");  // false (identicos, mesmo tipo)
var_dump(10 != "10");   // false (diferentes)
var_dump(10 !== "10");  // true  (nao identicos)

// --- Logicos ---
var_dump(true && false); // false (AND)
var_dump(true || false); // true  (OR)
var_dump(!true);         // false (NOT)

// --- Incremento/Decremento ---
$x = 5;
echo ++$x;  // pre-incremento: 6
echo $x++;  // pos-incremento: 6 (depois vira 7)

// --- Concatenacao (unir textos) ---
echo "Ola, " . "mundo!";  // Ola, mundo!

// --- Null Coalescing (PHP 7+) ---
$nome = $_GET['nome'] ?? 'visitante'; // se nao existir, usa padrao
*/ 
?>